# Dependencias de desarrollo y testing (NO se instalan en producción)
# Instalar con: pip install -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-cov>=5.0                  # Reporte de cobertura: pytest --cov=core tests/
coverage>=7.0                    # Code coverage analysis
coverage-badge>=1.1.0            # Coverage badge generation

# Calidad de código
ruff>=0.4                        # Linter + formatter (reemplaza flake8 + black)
mypy>=1.9                        # Type checking
bandit>=1.7                      # Security vulnerability scanner

# Pre-commit & Git hooks
pre-commit>=3.5                  # Framework para git hooks
pre-commit-framework>=3.5

# Code formatting & analysis
black>=24.0                      # Code formatter (alternative to ruff format)
isort>=5.13                      # Import sorting

# Performance profiling
psutil>=5.9                      # System and process monitoring
py-spy>=0.3                      # Sampling profiler (optional, advanced)

# Development tools
ipython>=8.0                     # Enhanced interactive shell
jupyter>=1.0                     # Jupyter notebook support
notebook>=7.0                    # Jupyter notebook

# Documentation
sphinx>=7.0                      # Documentation generator (optional, Fase 2+)
sphinx-rtd-theme>=2.0            # ReadTheDocs theme (optional)
